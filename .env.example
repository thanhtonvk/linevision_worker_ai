# Environment variables for Tennis Analysis API

# API Configuration
API_HOST=0.0.0.0
API_PORT=5000
DEBUG=True

# Model paths (relative to project root)
MODEL_DIR=models
BALL_MODEL_PATH=models/ball_best.pt
PERSON_MODEL_PATH=models/yolov8m.pt
POSE_MODEL_PATH=models/yolov8n-pose.pt

# Upload and output folders
UPLOAD_FOLDER=uploads
OUTPUT_FOLDER=outputs

# File cleanup settings (hours)
CLEANUP_HOURS=24

# Analysis default parameters
DEFAULT_BALL_CONF=0.7
DEFAULT_PERSON_CONF=0.6
DEFAULT_ANGLE_THRESHOLD=50
DEFAULT_INTERSECTION_THRESHOLD=100
DEFAULT_COURT_BOUNDS=100,100,400,500
